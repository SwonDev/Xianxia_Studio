/// <reference types="vite/client" />

// v0.1.46: injected by vite.config.ts (define block) from the workspace
// package.json so the browser-mode shim can show the same version as
// the Tauri webview.
declare const __APP_VERSION__: string;
