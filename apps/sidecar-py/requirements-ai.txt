# Heavy AI deps — installed after the wizard's first run.
# Platform-specific torch wheels are resolved by the installer (CUDA/MPS/CPU).
-r requirements.txt
torch>=2.5
git+https://github.com/huggingface/diffusers
transformers>=4.46
accelerate>=1.1
qwen-tts>=0.1
faster-whisper>=1.1
moviepy>=2.1
audiocraft>=1.3
scenedetect>=0.7
mediapipe>=0.10
opencv-python>=4.10
# v0.2.17 — AI-provenance watermark (Meta AudioSeal). Pure-python wheel
# (~63 KB); its deps (numpy/omegaconf/einops/torch) are already pulled by
# the stack above. DO NOT REMOVE: the best-effort /watermark phase 13
# self-heals via pip if missing, but shipping it here means zero first-run
# wait. Runs eager (TORCHDYNAMO_DISABLE) — the bundled runtime has no MSVC.
audioseal>=0.2
# ultralytics (YOLOv8) — only needed if you want full-body subject tracking
# instead of MediaPipe Face. Uncomment to enable:
# ultralytics>=8.3
